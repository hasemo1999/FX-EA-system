# KPI検収基準 — 統一版（v2.7修正案）
> 本資料は、発注仕様書 v2.6 の KPI・検収条件・テスト条件・例外時挙動を補足し、
表示を減らす
> 検収時の解釈違いを防ぐための正式補足資料とする。
---
## 1. KPI（検収基準）
### 1.1 必須KPI（検収合否）
以下を **検収合格の必須条件** とする。
| 指標 | 基準 | 備考 |
|------|------|------|
| **PF（Profit Factor）** | **≥ 1.20** | MT5バックテスト結果レポート基準 |
| **最大ドローダウン（MaxDD）** | **≤ 10%** | MT5バックテスト結果レポート基準 |
### 1.2 参考KPI（開発目標）
以下は **開発上の努力目標** とし、検収不合格の直接条件とはしない。
| 指標 | 目標 | 備考 |
|------|------|------|
| WFE（Walk-Forward Efficiency） | ≥ 0.70 | Walk-Forward 分析結果 |
| Walk-Forward 合格率 | ≥ 70% | 全ウィンドウ中の合格割合 |
| PF | 1.30 以上 | 上位目標 |
| MaxDD | 8% 以内 | より厳格なリスク管理目標 |
| トレード数 | 月 3〜10 回程度 | 過剰取引・過少取引の目安 |
### 1.3 数値の主語
1. KPI測定は **MT5 Strategy Tester のバックテスト結果レポート** を唯一の基準とする。
2. PF / MaxDD は **手数料・スプレッド・約定条件を反映した結果** を基準とする。
3. 複数回テストした場合は、**双方合意のテスト条件（第2項）で実行した結果** を基準とする。
4. WFEおよびWalk-Forward合格率は **参考指標** とし、Phase 1ではロジック優位性の補助評価に用いる。
5. WFE / Walk-Forward 合格率を将来「必須条件」に格上げする場合は、**窓数・分割比率・ロール方式・合格判定式** を別紙で固定することを前提とする。
---
## 2. 検収テスト条件（固定）
同じ EA でも条件が異なれば結果が変わる。以下を固定する。
### 2.1 テスター設定
| 項目 | 固定値 |
|------|--------|
| プラットフォーム | MT5 Strategy Tester |
| モデル | **Every tick based on real ticks** |
| 通貨ペア | USDJPY |
| 時間足 | H1 |
| 初期証拠金 | 1,000,000 JPY |
| レバレッジ | 1:25 |
| スプレッド | 原則として実測値（real ticks由来）を使用 |
| 手数料 | ブローカー標準設定に従う |
| 遅延 | 0 ms |
**補足**
- 固定スプレッドを使用する場合は、双方の書面合意がある場合に限る。
- 合意なき場合、変動スプレッド（実測）を正とする。
### 2.2 ブローカー / 口座条件
| 項目 | 指定 |
|------|------|
| ブローカー | OANDA MT5（または双方合意のブローカー） |
| 口座タイプ | デモ口座（検証用） |
| スワップ | テスター標準値を使用 |
### 2.3 ティックデータ
| 項目 | 指定 |
|------|------|
| データ出所 | MT5 内蔵データ（MetaQuotes 提供） |
| データ共有 | 発注者・受注者が **同一データソース** を使用する |
| 差異が出た場合 | 双方がデータの MD5 ハッシュを交換し、同一性を確認する |
### 2.4 テスト期間
| 区分 | 期間 |
|------|------|
| **バックテスト全体** | **2020/01/01 〜 2025/01/01** |
| **IS（In-Sample）** | 2020/01/01 〜 2022/12/31 |
| **OOS（Out-of-Sample）** | **2023/01/01 〜 2024/12/31** |
### 2.5 再現性確認
1. 同一条件で **3回連続実行** し、少なくとも以下の主要指標が一致することを確認する。
   - PF
   - MaxDD
   - 総取引数
2. 3回の結果に差異が出た場合は、**データ・設定・EAバージョン・口座条件** を再確認し、原因を記録する。
---
## 3. 例外時の挙動（決め打ち）
### 3.1 注文失敗時
| シナリオ | 挙動 |
|----------|------|
| リクオート / 約定拒否 | 最大 **3回** リトライ（間隔: 1秒, 2秒, 4秒） |
| スリッページ過大（許容値超過） | 当該注文を見送り、次バーで再判定 |
| 回線断 / サーバー無応答 | 30秒待機後リトライ × 3回。失敗時は次バーまで見送り |
| リトライ全失敗 | ログ記録＋通知、次バーで再判定 |
### 3.2 スプレッド急拡大時
| 項目 | 値 |
|------|-----|
| エントリー禁止スプレッド上限 | **5.0 pips** 超でエントリー禁止 |
| 禁止解除条件 | スプレッドが 5.0 pips 以下に戻った次バーの確定時 |
### 3.3 ポジション管理
| 項目 | 値 |
|------|-----|
| 最大同時ポジション数 | EA パラメータ `MaxPositions` に従う（デフォルト: 1） |
| 最大保有時間 | **72 時間**（超過時は成行決済） |
### 3.4 金曜停止
| 項目 | 値 |
|------|-----|
| 時間基準 | **サーバー時間** |
| 新規エントリー停止 | 金曜 サーバー時間 22:00 以降はエントリー禁止 |
| 保有ポジション | 金曜 サーバー時間 22:00 に全ポジション成行決済 |
| 補足 | JST 換算表を別途添付 |
---
## 4. 納品物の検収方法
### 4.1 納品物一覧（v2.6 準拠）
- mq5 / ex5 ファイル
- パラメータ設定ファイル（.set）
- 導入手順書
- ログ仕様書
- 正解チャート集
### 4.2 正解チャート集の検収基準
| 項目 | 基準 |
|------|------|
| 枚数 | 最低 **20 枚** |
| 期間 | OOS 期間（2023〜2024）から抽出 |
| パターン網羅 | Mode A: 最低 5 例、Mode B: 最低 5 例、見送り判定: 最低 5 例、決済判定: 最低 5 例 |
| 矢印位置判定 | シグナル確定バーを基準に一致確認 |
| 注文価格判定 | 実注文価格はスプレッド・約定条件の範囲内で許容 |
| エグジット判定 | 決済バー価格 **± 1 pip** を目安に確認 |
### 4.3 ログの最低要件
受注者は少なくとも以下をログに記録すること。
- 注文時刻
- 通貨ペア
- 売買種別
- ロット
- 注文価格
- SL / TP
- 拒否理由
- 再試行回数
- 決済時刻
- 決済価格
- 当該エントリーが Mode A / Mode B のいずれか
---
## 5. 契約面（改修範囲の線引き）
### 5.1 無償改修の定義
> **無償改修** とは、同一仕様の範囲内で、必須KPI達成のために必要なパラメータ調整・ロジック修正を行うことを指す。
### 5.2 別見積の定義
以下は **仕様変更** とみなし、別途見積とする。
- トレーリングストップ追加
- ロット可変ロジック追加
- 新規通貨ペア追加
- 新規フィルタ追加
- 新規通知方式追加
- Phase 1 スコープ外の機能全般
### 5.3 判断ルール
```
KPI 未達 → 同一仕様の範囲？
  ├─ YES → 無償改修（受注者負担）
  └─ NO  → 仕様変更として別途見積
```
---
## 6. 最終補足
本資料は、発注仕様書 v2.6 の KPI・検収・再現性・契約解釈を補足するためのものであり、
**本資料と発注仕様書 v2.6 に矛盾がある場合は、本資料を優先して解釈する。**
---
## 変更履歴
| 日付 | 変更内容 |
|------|----------|
| 2026-03-07 | 初版作成。v2.6 の KPI を必須/参考に分離・統一。テスト条件・例外挙動・検収方法・改修範囲を追記 |
| 2026-03-07 | v2.7修正。WFE/WF合格率を参考KPIに移動。スプレッドをreal ticks実測値に固定。再現性確認（3回連続実行）追加。正解チャート判定ルール修正。ログ最低要件追加。本資料の優先条項追加 |